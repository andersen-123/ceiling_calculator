# 📱 Сборка APK для Android

## 🚀 Быстрый старт

Выполните готовый скрипт:
```bash
./QUICK_BUILD.sh
```

## 🔧 Ручная сборка

Если скрипт не сработал, выполните команды по шагам:

### 1. Настройка окружения
```bash
export JAVA_HOME=/usr/lib/jvm/java-11-openjdk
export PATH="/usr/lib/jvm/java-11-openjdk/bin:$PATH:/home/ander/flutter/bin"
```

### 2. Очистка кэша
```bash
rm -rf ~/.gradle/caches
```

### 3. Установка зависимостей
```bash
cd /home/ander/CascadeProjects/ceiling_calculator
flutter pub get
```

### 4. Сборка APK
```bash
flutter build apk --release --no-shrink
```

## 📲 Установка APK на устройство

Через ADB:
```bash
adb install build/app/outputs/flutter-apk/app-release.apk
```

Или просто скопируйте файл на телефон и установите через файловый менеджер.

## ⚠️ Возможные проблемы

### Проблема: "Unsupported class file major version 65"
**Решение:** Убедитесь что используется Java 11, а не Java 25

### Проблема: "Android toolchain - develop for Android devices"
**Решение:** Установите Android SDK 36

### Проблема: "file_picker:linux references"
**Решение:** Это предупреждение можно игнорировать, не влияет на сборку

## 📁 Готовый APK

После успешной сборки файл будет здесь:
```
build/app/outputs/flutter-apk/app-release.apk
```

## 🎯 Особенности приложения

- ✅ Полнофункциональный калькулятор потолков
- ✅ Экспорт в PDF и Excel  
- ✅ Локальная база данных SQLite
- ✅ Отправка через мессенджеры
- ✅ Русский интерфейс
- ✅ Поддержка Android 5.0+

## 📞 Поддержка

Если возникнут проблемы с установкой Flutter SDK или сборкой:
1. Проверьте версию Java: `java -version`
2. Установите Android Studio для Android SDK
3. Используйте официальную документацию Flutter
